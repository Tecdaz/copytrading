# Changes — copytrading

Active changes live in this directory as `openspec/changes/{change-name}/`.
Completed changes are moved to `archive/YYYY-MM-DD-{change-name}/` by
`sdd-archive`.

## Status

| Change | Phase | Owner | Last update |
| --- | --- | --- | --- |
| _(none yet — first change is the bootstrap / tooling setup)_ | — | — | — |

## Recommended first change

`bootstrap-tooling` — uv init, lockfile, dev tooling (pytest, ruff, mypy),
CI skeleton, and a `copytrading` package skeleton. Without this, no other
change can be verified.
